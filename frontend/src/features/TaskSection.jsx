
const TaskSection = ({ tareas, setTareas }) => {

    return (
        <div className="container">
            <fieldset>
                <legend>Seleccionar tareas delegadas por IA generativa</legend>
                <div className="fieldset-container">
                </div>
            </fieldset>
        </div>
    )
};

export default TaskSection;